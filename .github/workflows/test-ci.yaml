name: Run - Test CI
on:
  push:
    branches:
      - 'test'
env:
  BASEURL: "https://example.cypress.io/todo"

jobs:
  Build:
    name: Run test
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    
    - name: Run npm i
      uses: mathiasvr/command-output@v1 
      id: installdependencies
      with:
        run: |
          npm i
          npm install https://github.com/LambdaTest/lambdatest-cypress-cli.git#devtesting
          npx lambdatest-cypress-cli run 
